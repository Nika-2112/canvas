/**
 * Конфигурация Tailwind CSS.
 * 
 * Поля content указывают, в каких директориях Tailwind должен искать классы.
 * Это необходимо для генерации только тех стилей, которые реально используются.
 */
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx}",       // директория с компонентами Next.js App Router
    "./components/**/*.{js,ts,jsx,tsx}" // директория с пользовательскими компонентами
  ],
  theme: {
    extend: {}, // при необходимости можно расширить тему (цвета, шрифты и т.п.)
  },
  plugins: [],
};
